services:
  - type: worker
    name: anti-duplicate-bot
    runtime: python

    buildCommand: |
      pip install -r requirements.txt

    startCommand: |
      python main.py

    envVars:
      - key: BOT_TOKEN
        sync: false
